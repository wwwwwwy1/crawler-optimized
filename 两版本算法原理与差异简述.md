# Original 与 Optimized 两版算法简述

## 核心逻辑变动

```text
Original：Activity 驱动的有界递归遍历 + dHash 页面布局指纹
Optimized：状态感知的有界遍历 + 页面模板控制 + SHA-256/pHash 图片去重
```

最初的算法为“BFS + dHash”，但 Android 原代码实际采用按点击路径递归深入的方式，更接近**受深度限制的 DFS**，且在安卓机上测试后，depth=1时效果是最好的

## 1. Original 算法

### 原理

- 主要用 `Activity` 判断是否进入新页面
- 将控件树绘制成布局骨架，再计算 dHash，作为页面布局指纹
- 使用最大深度、动作数、截图数和运行时间限制遍历范围

### 运作方式

```text
切换首页 Tab
→ 截图当前页
→ 获取可点击控件
→ 逐个点击
→ 固定等待
→ Activity 变化则递归探索子页面
→ 返回父页面继续点击
```

### 容易影响效率的地方

- 同一 Activity 内的 Tab、Fragment、弹窗和内容切换容易被漏掉
- 视频详情页的推荐链会产生大量同模板页面，深入和回退成本较高
- dHash 表示的是 UI 布局骨架，不等于对实际截图做交付去重
- 截图流程不直接拒绝近重复图片，重复数据需要后处理
- 固定等待和频繁 Poco 属性读取带来额外耗时

## 2. Optimized 算法

### 原理

优化之后的算法，即Optimized，将“是否为新页面”和“截图是否重复”拆成两个独立问题：

- `state_key`：由 Activity 和规范化控件树生成，判断是否为新的 UI 状态
- `template_key`：忽略动态文本和列表内容，识别是否为同一种页面模板
- `SHA-256 + pHash`：分别识别完全相同图片和视觉近重复图片

### 运作方式

```text
一次 dump 获取控件树
→ 生成 state_key 和 template_key
→ 跳过已经探索的状态
→ 截图并执行 SHA-256/pHash 去重
→ 保存有效图片和 Metadata
→ 从控件树本地提取动作并按价值排序
→ 点击后进行分级稳定等待
→ 发现新状态则截图
→ 返回并验证父 Activity
```

当前b站实测默认 `MAX_DEPTH=1`：优先完成首页、各 Tab 和一层子状态的横向覆盖。深度 2/3 保留为针对特定 App 或模板单独开启的策略，且后续需要在别的app上测试效率

ps：群里说的max_depth的问题，我做过一些能够从中间操作省时间的办法，但实测都不太好，跟depth为1差挺多，后续需要在其它app测试一下，可以不同app设定不同的max_depth

## 3. 核心差异

| 对比项 | Original | Optimized |
|---|---|---|
| 页面识别 | 主要依赖 Activity | Activity + 控件树 `state_key` |
| 模板识别 | 缺少独立模板键 | `template_key` 控制同模板推荐链 |
| 遍历方式 | 按点击路径递归深入 | 状态感知、有预算的横向优先采集 |
| 同 Activity 变化 | 容易漏掉 | 可以识别 |
| 动作获取 | 多次读取 Poco 节点属性 | 一次 dump 后本地提取 |
| 页面等待 | 固定 sleep | Activity 优先、控件树按需检查 |
| 图片去重 | dHash 布局指纹，截图后处理 | SHA-256 精确去重 + pHash 近重复去重 |
| 防止失控 | 深度、动作、截图、时间限制 | 再增加状态数和模板连续深入限制 |
| 运行分析 | 指标较少 | 记录状态、等待、去重、回退和重启指标 |

## 4. 为什么这样优化

1. **提高覆盖准确性**：不再只看 Activity，能够识别同一 Activity 内真实发生的 UI 状态变化
2. **减少无效深入**：模板识别可以阻止视频详情页推荐链反复深入
3. **提高采集速度**：减少 Poco RPC、固定等待和无效回退，把时间用于产生新状态和有效截图
4. **提高图片有效率**：实际截图在写入 Metadata 前完成精确去重和近重复去重
5. **提高稳定性**：深度、状态、动作、截图、模板和时间共同限制动态 App 的状态空间

最终 Android AB/BA 实测中，Optimized 相比 Original：

- 截图吞吐提高 **60.1%**；
- dHash 评估的去近重图片吞吐提高 **82.0%**；
- 唯一 Activity 中位数提高 **20.0%**；
- App 重启次数中位数降低 **83.3%**
- ps：这块是我根据项目需求理解的，如果有别的指标后续还可以补充
